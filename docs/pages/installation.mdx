import Layout from "../layouts/main";

export const meta = {
  title: "Installation",
};

# Installing from npm

React Form is available to install from NPM you can run one of the below
commands to install it with your favourite package manager.

```bash
yarn add @adeattwood/react-form
```

```bash
npm install @adeattwood/react-form
```

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
